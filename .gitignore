# macOS
.DS_Store
._*

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo

# Logs / temp
*.log
*.tmp
/tmp/worldclock_server.pid

# Stoa wake monitor since-id checkpoint (per-worktree runtime artifact)
.stoa-since-*

# Python (백엔드 도입 시 활성)
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node (프론트 도구화 시 활성)
node_modules/
dist/
build/
.next/
.cache/
coverage/

# 환경 변수 / 비밀
.env
.env.local
.env.*.local

# OS
Thumbs.db

# 멤버 워크트리는 루트 repo의 형제(<parent>/<멤버이름>/)에 위치 — repo 외부라 .gitignore 불필요 (CLAUDE.md 규칙 16)

# Stoa wake monitor since_id 영속 파일 (per-worktree, ONBOARDING §1.0)
.stoa-since-*
